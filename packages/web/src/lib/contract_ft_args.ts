// ================================================
export interface ft_args_params_interface {
  account_id: string;
  receiver_id: string;
  amount: string;
  msg: string;
}
// ================================================
